  CREATE TABLE JOBBOARD(	
    JNO NUMBER(10) PRIMARY KEY, 
	ID VARCHAR2(20) REFERENCES USERS(ID), 
	TITLE VARCHAR2(50) NOT NULL, 
	CONTENT VARCHAR2(500) NOT NULL, 
	REGDATE DATE
);

create SEQUENCE JOBBOARD_seq NOCACHE;